---
import { getEntry, render } from 'astro:content';
import BaseLayout from '@/layouts/BaseLayout.astro';
import AboutMenu from '@/components/AboutMenu.astro';

const entry = await getEntry('about', 'editorial-policy');
if (!entry) throw new Error('Missing about/editorial-policy.md content entry');
const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title}>
  <main id="maincontent" class="wrapper">
    <h1>{entry.data.title}</h1>
    <Content />
  </main>
  <AboutMenu currentPath="/about/editorial-policy" />
</BaseLayout>
